# include <stdio.h>

int main() {
    printf("Hello world!\n");

    for(int x=1; x<10; x++) {
        printf("%d\n", x); // This should print the first/only variable x as an integer        
    }

    // Add a method for reordering a number from smallest to largest digits
    // 2390 would become 0239 as the output

    return 0;
}